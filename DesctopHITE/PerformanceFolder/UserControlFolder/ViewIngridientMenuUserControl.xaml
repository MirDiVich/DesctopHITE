<UserControl 
    x:Class="DesctopHITE.PerformanceFolder.UserControlFolder.ViewIngridientMenuUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d">
    <StackPanel Orientation="Vertical">
        <TextBlock
            Text="{Binding Name_Ingredients}"
            Style="{DynamicResource MainStyleTextBlock}"
            TextAlignment="Left"
            HorizontalAlignment="Left"
            Margin="0 0 10 0"
            FontSize="18"/>
        <StackPanel Orientation="Horizontal" Margin="10 0 0 0">
            <TextBlock
                Text="кол-во использований:"
                Style="{DynamicResource MainStyleTextBlock}"
                TextAlignment="Left"
                HorizontalAlignment="Left"
                Margin="0 0 2 0"
                FontSize="15"/>
            <TextBlock
                Text="{Binding CountIngridientMenu}"
                Style="{DynamicResource OtherStyleTextBlock}"
                TextAlignment="Left"
                HorizontalAlignment="Left"
                Margin="2 0 0 0"
                FontSize="15"/>
        </StackPanel>
    </StackPanel>
</UserControl>

<UserControl 
    x:Class="DesctopHITE.PerformanceFolder.UserControlFolder.ListBasketUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image
            Grid.Column="0"
            Source="{Binding MenuTable.ImageMenuTable.Image_ImageMenu}"
            Width="150"
            Stretch="Uniform"
            Height="150"/>
        <Grid Grid.Column="1">
            <TextBlock
                Style="{DynamicResource MainStyleTextBlock}"
                FontSize="30"
                Text="{Binding MenuTable.Name_Menu}"
                Margin="10 0 0 0"/>
        </Grid>
    </Grid>
</UserControl>
